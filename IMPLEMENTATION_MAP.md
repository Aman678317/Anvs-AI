# ANVS-AI Multilingual Meeting Platform — Implementation Map (Stage 0)

> **Document Version**: 4.0.0  
> **Evaluation Date**: September 24, 2026  
> **Requirement Schema**: `PDF Requirement` → `Current File/Service` → `Current Implementation` → `Status` → `Missing Work` → `Target PR` → `Target Step` → `Test Required` → `Runtime Evidence Required`

---

## Complete Requirement-to-Code Traceability Matrix

| PDF Requirement                            | Current File/Service                                                                          | Current Implementation                                                                                                                       | Status       | Missing Work                                                               | Target PR | Target Step | Test Required                                                                                    | Runtime Evidence Required                                                                   |
| ------------------------------------------ | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | -------------------------------------------------------------------------- | --------- | ----------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| **1. Multi-Tenant Isolation & RLS**        | `packages/database/session.py`, `packages/database/models/`                                   | PostgreSQL 16 RLS policies with `SET LOCAL app.current_tenant_id` on all 14 tables                                                           | **COMPLETE** | None in data layer; verify WSS and LiveKit grant isolation                 | PR-04     | STEP-04-2   | Cross-tenant negative queries (`SELECT`, `INSERT`, `UPDATE`, `DELETE`)                           | PostgreSQL transaction query log showing RLS boundary denial                                |
| **2. Immutable Source Segment Lineage**    | `packages/event_schema/events.py`, `packages/contracts/src/events.ts`                         | Enriched `BaseEvent` with v1.2 lineage (`causation_id`, `parent_event_id`, `hop_count`, `max_hops`, `ttl_seconds`, and golden JSON fixtures) | **COMPLETE** | None (Validated via golden schema fixtures and causal DAG tests)           | PR-06     | STEP-06-1   | `test_immutable_source_segment_lineage_chain`, `test_golden_schema_v12_serialization_parity`     | Golden fixtures and contract tests demonstrating unbroken causal DAG                        |
| **3. 20 kHz Ultrasonic Watermarking**      | `packages/audio/watermark.py`, `services/tts_worker/engine.py`, `packages/audio/ingestion.py` | FFT-based 20,000 Hz tone insertion and detection with 3.0 SNR threshold                                                                      | **COMPLETE** | LiveKit audio subscriber hook to feed into watermark detector              | PR-10     | STEP-10-2   | Unit test `test_watermark_detection` and loop rejection test                                     | Audio spectrum analysis showing 20kHz notch and zero synthetic frames admitted to STT       |
| **4. Poison-Pill DLQ Quarantine**          | `services/orchestrator/dlq_retry.py`, `services/orchestrator/pipeline.py`                     | `DLQRetryManager` enforces 3 retry budget with exponential backoff and quarantine stream                                                     | **COMPLETE** | Stream trimming policy tuning                                              | PR-07     | STEP-07-2   | Chaos test `test_dlq_quarantine_after_exhaustion`                                                | Redis Stream `meeting:{id}:dlq` entry verification                                          |
| **5. Global Language Strategy & Registry** | `packages/language_registry/languages.py`                                                     | Multi-tier catalog with Indic routing (DEC-01), licensing, and checkpoints (`ai4bharat/indictrans2-1B`, NLLB-200)                            | **COMPLETE** | None (Validated via language capability registry test suite)               | PR-11     | STEP-11-1   | Capability registry schema and validation tests                                                  | Registry dump showing exact checkpoints (IndicTrans2, NLLB-200) and statuses                |
| **6. Concurrent Translation Fan-Out**      | `services/translation_worker/engine.py`, `services/translation_worker/consumer.py`            | Fan-out translating source segment into all listener languages with glossary protection and v1.2 lineage                                     | **COMPLETE** | None (Validated via listener resolver and glossary tests)                  | PR-11     | STEP-11-2   | Multi-target fan-out unit test ($S \to T_{en}, T_{mr}, T_{ja}$)                                  | Redis Streams showing simultaneous translation events for different targets from one source |
| **7. Real LiveKit Media Plane**            | `services/api/services/livekit_service.py`                                                    | Twirp API room operations, participant listing, HMAC webhook verification, and prod fail-fast                                                | **COMPLETE** | None (Validated via LiveKit service unit and error propagation tests)      | PR-09     | STEP-09-1   | Integration test with real/mock LiveKit server; negative error propagation test                  | LiveKit Room Service API HTTP 200 response and verified room SID                            |
| **8. Real LiveKit Audio Egress**           | `services/tts_worker/egress.py`                                                               | `livekit.rtc.AudioSource` and `LocalAudioTrack` publisher with audience-scoped routing                                                       | **COMPLETE** | None (Validated via egress track creation and WebRTC frame push tests)     | PR-12     | STEP-12-1   | Egress track creation and WebRTC frame push unit test                                            | LiveKit CLI/SDK receiving live synthetic audio track                                        |
| **9. Human Audio Ingress & Source Gate**   | `services/audio_ingress/subscriber.py`, `services/audio_ingress/service.py`                   | `LiveKitAudioSubscriber` enforces Human Source Gate (Invariant #1), 48kHz watermark loop check, 16kHz resampling                             | **COMPLETE** | None (Validated via unit tests and Invariant #3 loop rejection suite)      | PR-10     | STEP-10-1   | `test_human_source_gate_filtering`, `test_subscriber_end_to_end_invariant_3_watermark_rejection` | Real WebRTC frame ingress, watermark echo drop, and stream dispatch to Redis                |
| **10. Evolving WebSocket State & Resync**  | `services/realtime_gateway/server.py`, `services/realtime_gateway/manager.py`                 | Monotonic state version sequence per room, 100-frame ring buffer, and `WSClientResyncFrame` handling                                         | **COMPLETE** | None (Validated via monotonic sequence and gap recovery tests)             | PR-08     | STEP-08-1   | Resync protocol unit test simulating packet drop and reconnect                                   | WebSocket frame capture demonstrating client state resync after reconnect                   |
| **11. Streaming STT & Language Detection** | `services/stt_worker/engine.py`, `services/stt_worker/consumer.py`                            | Faster-Whisper streaming engine with language detection, confidence scoring, and canonical v1.2 lineage envelope                             | **COMPLETE** | None (Validated via audio stream consumption and envelope causation tests) | PR-10     | STEP-10-3   | `test_stt_consumer_envelope_causation_propagation`, `test_stt_consumer_audio_decoding`           | Live transcript output matching spoken audio with language tag and unbroken causal DAG      |
| **12. Deterministic Timing & Stale-Drop**  | `services/tts_worker/consumer.py`, `services/tts_worker/egress.py`                            | Queue deadline check: if $(T_{now} - T_{source}) > \text{threshold}$, drops late audio and logs `AUDIO_STALE_DROPPED`                        | **COMPLETE** | None (Validated via timing engine stale drop unit tests)                   | PR-12     | STEP-12-3   | Timing engine negative test dropping late segments                                               | Log event `AUDIO_STALE_DROPPED` when latency exceeds 2500ms                                 |
| **13. Multilingual Persistent Chat**       | `packages/database/models/meeting_chat.py`, `services/realtime_gateway/`                      | Chat frame broadcasting and database model exist; chat translation not persisted                                                             | **PARTIAL**  | Persist both original and translated text; add chat history resync         | PR-14     | STEP-14-1   | Chat persistence and multi-language translation test                                             | Reconnected client receiving authoritative chat history                                     |
| **14. Grounded AI Assistant & RAG**        | `services/assistant_worker/`                                                                  | QueryRouter, in-memory vector store, stream summarizer, LLM mock fallback                                                                    | **COMPLETE** | Seven-type memory integration; tenant-scoped document retrieval            | PR-14     | STEP-14-3   | Assistant intent classification and citation grounding tests                                     | Assistant answering question using only meeting transcript facts                            |
| **15. Voice Cloning & Consent Gating**     | `services/voice_worker/`                                                                      | Voice cloning engine, user consent verification, pgvector profile matching                                                                   | **COMPLETE** | Connect to live audio egress                                               | PR-11     | STEP-11-4   | Consent gate negative test (prohibit cloning without verified consent)                           | Profile embedding cosine similarity match log                                               |
| **16. Production Config & Mock Policy**    | `packages/config/settings.py`                                                                 | Defaults to `"mock"` engines across STT, NMT, TTS, Speaker workers                                                                           | **UNSAFE**   | Split dev/test/prod profiles; fail fast in production if mock selected     | PR-01     | STEP-01-4   | Configuration validation test rejecting mock engines in production                               | Startup exception trace when `APP_ENV=production` and `STT_ENGINE_TYPE=mock`                |
